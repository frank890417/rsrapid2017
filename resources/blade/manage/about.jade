@extends('layouts.app_manage')
@section('content')
.row
  .col-lg-12
    .row
      ol.breadcrumb
        li
          a(href='#')
            svg.glyph.stroked.home
              use(xlink:href='#stroked-home')
        li 
          a(href="#") 關於睿軒
        
    @yield('content')
  .col-lg-12
    h1.page-header 關於睿軒
      .btn.btn-danger.pull-right(@click="save_website_info") 儲存更新

.row(v-if="lang.zh")
  .col-lg-12
    .panel.panel-primary
      .panel-heading 頁面內容管理
      .panel-body
        .form-group
          label 內容
          tiny-mce(id = "about_section1_content" , v-model="lang.zh.page_about.section_1.content",
          :other-props="mce_plugin",  :toolbar="mce_toolbar")
.row
  .col-lg-6
    .panel.panel-primary
      .panel-heading 年表管理
      .panel-body
        
        table.table.table-hover
          thead
            th 編號
            th 年份
            th 月份
            th 標題
            //- th 內容
            th 連結新聞
            //- th 更新時間
            //- th 編輯
            th 操作
          tbody 
            tr(v-for="(yearlog,id) in yearlogs")
              td(style="width: 5%") @{{yearlog.id}}
              td(style="width: 7%") @{{yearlog.year}}
              td(style="width: 7%") @{{yearlog.date}}
              td(style="width: 20%") @{{yearlog.title}}
              //- td(style="width: 40%") @{{yearlog.content}}
              td(style="width: 10%") @{{yearlog.news_id==-1?'無':yearlog.news_id}}
              //- td(style="width: 10%") @{{yearlog.updated_at}}
              td(style="width: 5%")
                .btn.btn-default(@click="now_yearlog=id") 編輯
              td(style="width: 5%")
                button.btn.btn-danger.btn-md(@click!="delete_yearlog(yearlog.id)") 刪除
                //- form(:id="'delete_solution_'+yearlog.id", :action="'/manage/yearlog/'+yearlog.id", method='POST')
                //-   input(type='hidden', name='_method', value='delete')
                //-   input(type='hidden', name='_token', value='{{ csrf_token() }}')
        br
        a.btn.btn-primary(href!="{{ url('manage/yearlog/create') }}") 新增事件
        br
  .col-lg-6
    .panel.panel-default(v-for="(yearlog,id) in [yearlogs[now_yearlog]]")
      .panel-heading 編輯中-@{{yearlog.id}}@{{yearlog.title}}
      .panel-body
        label 編號@{{yearlog.id}}
        table.table
          tr
            td
              label 年份
            td
              input.form-control(v-model="yearlog.year")
          tr
            td
              label 月份
            td
              input.form-control(v-model="yearlog.date")
          tr
            td
              label 標題
            td
              input.form-control(v-model="yearlog.title")
          tr
            td
              label 內容
            td
              textarea.form-control(v-model="yearlog.content",rows=4)
          tr
            td
              label 連結新聞
            td
              select.form-control(v-model="yearlog.news_id")
                option(value="-1") 未連結
                option(v-for="a_news in news",:value="a_news.id") @{{a_news.title}}
          tr
            td
              label 更新時間
            td
              input.form-control(v-model="yearlog.updated_at")

        br
        a.btn.btn-primary(href!="{{ url('manage/yearlog/create') }}") 新增事件
        br


@endsection
